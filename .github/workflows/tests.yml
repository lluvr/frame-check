name: Tests

# Runs the full Frame Check test suite on PRs and on manual dispatch.
# The existing runner is run_tests.py which wraps the
# standalone-script test pattern documented in CONTRIBUTING.md. Rather
# than migrate to pytest in one move, CI respects the existing pattern.
#
# Push-on-master trigger removed 2026-05-07 alongside manual-deployment
# discipline: the operator runs pytest + lift_dry_run locally before
# every push, and the release orchestrator gates a release on the
# same lift_dry_run + conformance driver. PR-time CI catches changes
# from contributors before merge; master-push CI duplicated work
# already done locally at billing cost. Re-enable via workflow_dispatch
# when ad-hoc verification is wanted.

on:
  pull_request:
  workflow_dispatch:

# Least-privilege token. Both jobs only check out code and run
# tests; they do not push, comment, or touch other repo state.
# Declaring read-only explicitly pins this even if the repo-level
# default ever shifts back to write-all.
permissions:
  contents: read

# Opt into Node.js 24 for the JavaScript-based actions (checkout,
# setup-python). GitHub forces this default on June 2nd, 2026; opting
# in early removes the deprecation warning from every CI log and
# verifies the actions still work under the newer runtime now,
# rather than at the deadline. Action versions (@v4 / @v5) stay the
# same; only the underlying Node runtime changes.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  # A new push to the same branch cancels the previous run. Prevents
  # CI queue buildup when a contributor pushes corrections quickly.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    strategy:
      fail-fast: false
      matrix:
        # The wheel's pyproject `requires-python = ">=3.10"` commits
        # to compatibility across 3.10, 3.11, 3.12. CI verifies that
        # commitment on every push so the claim does not silently
        # drift. Production (Fly Dockerfile) runs 3.12. The 3.10/3.11
        # rows act as compatibility witnesses; failure on the older
        # rows is an honest signal to either bump the floor or fix
        # the incompatibility.
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          # Install the package + its [test] extras (pytest,
          # pytest-timeout, pytest-cov) declared in pyproject.toml
          # [project.optional-dependencies]. The legacy
          # requirements.txt was retired at v0.9.1; pyproject is the
          # single dependency source.
          python -m pip install --upgrade pip
          pip install -e ".[test]"

      - name: Run test suite
        # Run on the matrix Python version. Coverage instrumentation
        # is added on 3.12 only (one matrix row produces the report;
        # the others verify compatibility). The 65% floor is on
        # production code only (tests/ + setup.py + run_tests.py
        # excluded via [tool.coverage.run] omit in pyproject.toml).
        # Current is 69% with the source_network.py 10% module
        # dragging the average down; v1.0 contract raises to 80% on
        # the public wheel surface per ROADMAP.md.
        run: |
          if [ "${{ matrix.python-version }}" = "3.12" ]; then
            python3 run_tests.py \
              --cov=. --cov-report=term --cov-fail-under=65
          else
            python3 run_tests.py
          fi

  quality:
    # Quality-gate job: ruff (style + bug-prone-pattern lints) and
    # mypy (type-check the public wheel surface). Configuration in
    # pyproject.toml ``[tool.ruff]`` / ``[tool.mypy]``.
    #
    # ruff is strict-blocking (zero lints across the codebase under
    # the configured select families). The lenient ``[tool.mypy]``
    # config is also strict-blocking on the public wheel surface
    # (mcp_server, mcp_compose, mcp_resources, mcp_schema, framing,
    # comparison, clarethium_measure). The ``mypy --strict`` pass
    # (the v1.0 contract gate) runs as an informational ``|| true``
    # step until the remaining ~348 errors close.
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install quality tools
        run: |
          python -m pip install --upgrade pip
          pip install ruff mypy
      - name: Ruff lint
        # Strict-blocking. The codebase passes the configured
        # ``[tool.ruff.lint]`` select families (E, F, B, ISC) with
        # zero remaining lints; a new violation should fail PR.
        run: ruff check .
      - name: Mypy type-check (public wheel surface)
        # Lenient config: PR-blocking on the public wheel surface.
        run: |
          mypy mcp_server.py mcp_compose.py mcp_resources.py \
               mcp_schema.py framing.py comparison.py \
               clarethium_measure.py
      - name: Mypy --strict (v1.0 progress meter)
        # Strict pass surfaces the v1.0-contract progress as a
        # count, not as a 295-line wall of errors. Currently 295
        # remaining (mostly ``[no-untyped-call]`` cascading from
        # ``[no-untyped-def]``); the count drops as untyped
        # callees get annotated. Informational until v1.0.
        run: |
          mypy --strict mcp_server.py mcp_compose.py \
               mcp_resources.py mcp_schema.py framing.py \
               comparison.py clarethium_measure.py 2>&1 \
            | tail -3 || true

  pytest-smoke:
    # Secondary job: direct pytest invocation. Some suites
    # (test_circuit_breaker.py, test_phase1_load.py, test_construct_honesty_voice.py)
    # are idiomatic pytest and run cleanly under direct invocation.
    # Keeps the two invocation paths both verified.
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[test]"

      - name: Run pytest (idiomatic suites)
        run: |
          # Direct-pytest invocation of suites that are idiomatic
          # pytest (vs the standalone-script pattern most upstream
          # tests follow). Keeps both invocation paths verified.
          # Excluded from this list: tests that depend on optional
          # web-only modules absent from the wheel; the primitives
          # they exercise are pinned by test_framing_validation.py
          # and the frame-library suite.
          # --cov=. produces a coverage report against every Python
          # module exercised by the three suites. --cov-report=term
          # prints a line-coverage summary in the CI log so a
          # reviewer can read it without clicking into the run.
          # No external service (codecov.io etc.) is wired here:
          # the report lives in the workflow log and the discipline
          # is to keep the percentage stable or rising. A future
          # operator who wants a public badge can wire codecov-action
          # in a follow-up; the in-log report is the floor.
          python3 -m pytest -q \
            --cov=. --cov-report=term \
            tests/test_framing_validation.py \
            tests/test_frame_library.py \
            tests/test_mcp_server.py
